-Create unique id for each object in the world.
-Add a String In for userdata on shapes and bodies.
-Implement the polygon node.
-Add a CreateBody (Advanced) node, to allow multiple shapes per body.
-Add a = (or select) node for shapes and bodies.
-Add AABB queries, tu return [shape object filtered | shape index]
-Add Node to destroy body/shape
-Joints implementation.